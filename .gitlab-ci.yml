image: "python:3.7"

before_script:
  - python --version

stages:
  - backend_testing

backend:
  stage: backend_testing
  script:
  - pip install -r requirements.txt
  - python backend/DB_Load/testDBloader.py